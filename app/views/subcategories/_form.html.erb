<%= form_with(model: subcategory) do |form| %>
  <% if subcategory.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>

      <ul>
        <% subcategory.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :subcategory_name, class: "form-label" %>
    <%= form.text_field :subcategory_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :category_id, style: "display: block" %>
    <%= form.collection_select :category_id, Category.all, :id, :category_name, prompt: "-Select-" %>
  </div>

  <div>
    <%= form.submit "Create Subcategory", class: "btn btn-primary mt-3" %>
  </div>
<% end %>
