<div class="container mt-3">
  <h1><%= t("heading.order.edit") %></h1>

  <%= render "form", order: @order %>

  <br>

  <div class="d-flex align-items-center gap-2">
    <%= link_to t("buttons.order.show_order"), @order, class: "btn btn-primary" %> |
    <%= link_to t("buttons.order.back_orders"), orders_path, class: "btn btn-secondary"  %>
  </div>
</div>
