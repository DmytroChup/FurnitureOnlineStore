<div class="container mt-3">
  <h1>New order</h1>

  <%= render "form", order: @order %>

  <br>

  <div>
    <%#= link_to "Back to orders", orders_path, class: "btn btn-secondary"  %>
    <%= link_to "Back to orders", orders_path(user_id: current_user.id), class: "btn btn-secondary" %>
  </div>
</div>
